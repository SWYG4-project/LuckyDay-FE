import styled from "@emotion/styled";
import { css } from "@emotion/react";

export const Header = styled.div`
  ${() => css`
    display: flex;
    align-items: center;
    width: 100%;
    height: 100px;
  `}
`;
